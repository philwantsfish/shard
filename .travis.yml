language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"

# install dependencies
install:
  - cd secondary_implementations/python/
  - pip install -r test-requirements.txt

# run tests
script: nose2 -v 
